# Public build-time locations for the HTTP API and WebSocket endpoint.
# Omit these in proxied/same-origin development, or set deployment-specific
# absolute URLs when the frontend and backend are hosted separately.

VITE_API_BASE_URL=http://localhost:5000
VITE_WEBSOCKET_URL=ws://localhost:5000/ws
